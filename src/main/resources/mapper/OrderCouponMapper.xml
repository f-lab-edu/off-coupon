<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.offcoupon.repository.mysql.OrderCouponRepository">

    <insert id="save" parameterType="OrderCoupon">
        INSERT INTO order_coupon (order_id, coupon_id, discount_amount, created_at, updated_at)
        VALUES (#{orderDetailId}, #{couponId}, #{discountAmount},
                #{createdAt}, #{updatedAt});
    </insert>
</mapper>